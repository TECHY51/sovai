# Sandbox Execution Directory

Contains temporary workspaces and isolation configuration for the Python Sandbox Subsystem (Phase 6).
Executions here run with zero network access and strict CPU/memory limits.
Contents inside `workspace/` and `temp/` are transient and automatically scrubbed.
